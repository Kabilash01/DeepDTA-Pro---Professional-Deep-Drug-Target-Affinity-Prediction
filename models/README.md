# Models Directory

This directory contains trained models:

- `sample_model.pth` - Example pre-trained model
- `baseline_models/` - Baseline comparison models

**Note**: Large model files (*.pth, *.pkl) are gitignored to keep repository size manageable.